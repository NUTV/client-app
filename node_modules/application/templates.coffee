ehbs = require('express3-handlebars')

module.exports = (app) ->
    
    config =
        extname: 'hbs'
        defaultLayout: 'main.hbs'
    
    app.engine('hbs', ehbs(config))
    app.set('view engine', 'hbs')