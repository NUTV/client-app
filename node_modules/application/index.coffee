app         = require('express')()
routes      = require('application/routes')
templates   = require('application/templates')
staticFiles = require('application/staticFiles')

staticFiles(app)
templates(app)
routes(app)

console.info('server listening on port ' + process.env.PORT)
app.listen(process.env.PORT)