showsController = require 'application/controllers/showsController'

module.exports = (app) ->
  app.get '/shows', showsController.shows
  app.get '/shows/:name', showsController.show
  app.get '/shows/:name/:id', showsController.episode
