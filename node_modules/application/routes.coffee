showsController = require 'application/controllers/showsController'

module.exports = (app) ->
  app.get '/', showsController.shows
  app.get '/:name', showsController.show
  app.get '/:name/:episode', showsController.episode
