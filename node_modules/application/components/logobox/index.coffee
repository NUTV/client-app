Handlebars = require 'handlebars'
fs         = require 'fs'

module.exports = ->
  template = fs.readFileSync(__dirname + '/template.hbs', {encoding: 'utf8'})
  compiled = Handlebars.compile(template)
  return new Handlebars.SafeString(compiled())
