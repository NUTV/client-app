request = require('superagent')

show = () ->

  self = {}

  self.findByName = (name, callback) ->
    request
      .get('localhost:3000/shows/' + name)
      .end (err, res) ->
        callback res.body

  self

module.exports = show()
